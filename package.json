{
  "name": "ihs-hackspace-website",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "serve": "astro build && firebase serve",
    "deploy": "astro build && firebase deploy --only hosting:ihs-hackerspace-website",
    "deploy:example": "astro build && firebase deploy --only hosting:example-ihs-hackerspace-website"
  },
  "devDependencies": {
    "@astrojs/mdx": "^0.8.3",
    "@astrojs/svelte": "^1.0.2",
    "astro": "^1.0.3",
    "rehype-external-links": "^2.0.0"
  },
  "dependencies": {
    "astro-icon": "^0.7.3",
    "reading-time": "^1.5.0",
    "svelte": "^3.52.0"
    
  }
}
